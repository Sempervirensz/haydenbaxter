import type { Metadata } from "next";
import DetailLab from "@/components/DetailLab";

export const metadata: Metadata = {
  title: "Detail Layout Lab",
};

export default function DetailLabPage() {
  return <DetailLab />;
}
