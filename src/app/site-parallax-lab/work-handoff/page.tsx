import WorkHandoffSample from "@/components/site-parallax-lab/WorkHandoffSample";

export const metadata = {
  title: "Sticky Depth Handoff — Work Cards 01–04",
  robots: { index: false },
};

export default function WorkHandoffSamplePage() {
  return <WorkHandoffSample />;
}
