import WorkSectionCinematic from "@/components/work/WorkSectionCinematic";

export const metadata = {
  title: "Work — CD scroll + cinematic cards (merged preview)",
  robots: { index: false },
};

export default function WorkMergedPage() {
  return <WorkSectionCinematic />;
}
