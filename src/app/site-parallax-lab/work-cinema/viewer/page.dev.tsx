import ResponsiveViewer from "@/components/site-parallax-lab/ResponsiveViewer";

export const metadata = {
  title: "Cinematic Work Stack — responsive viewer",
  robots: { index: false },
};

export default function WorkCinemaViewerPage() {
  return <ResponsiveViewer />;
}
