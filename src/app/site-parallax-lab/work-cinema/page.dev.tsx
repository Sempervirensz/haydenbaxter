import CinematicWorkStack from "@/components/work/CinematicWorkStack";

export const metadata = {
  title: "Cinematic Work Stack — all four chapters, full bleed",
  robots: { index: false },
};

export default function WorkCinemaPage() {
  return <CinematicWorkStack lab />;
}
